## Install

```bash
## Clone
git clone https://github.com/promptengineers-ai/exec-server.git && cd exec-server

## Install Modules
tmux new-session -d -s exec_server "npm install && node index.js"
```

